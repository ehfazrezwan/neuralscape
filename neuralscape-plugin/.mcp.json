{
  "mcpServers": {
    "neuralscape": {
      "type": "http",
      "url": "${CLAUDE_PLUGIN_OPTION_URL}/mcp/",
      "headers": {
        "Authorization": "Bearer ${CLAUDE_PLUGIN_OPTION_API_KEY}"
      }
    }
  }
}
